#include "Frazione.h"

Frazione::Frazione(int num,int den) {
    this->num = num;
    this->den = den;
}

int Frazione::getNum(){(return num;)}
int Frazione::getDen(){(return den;)}

void Frazione::setNum(num){(this->num=num;)}
void Frazione::setDen(den){(this->den=den;)}